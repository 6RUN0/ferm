domain eb {
    table filter {
        chain INPUT {
            policy ACCEPT;
            interface eth0 ACCEPT;
        }
        chain FORWARD policy DROP;
        chain OUTPUT policy ACCEPT;
    }
    table nat {
        chain PREROUTING {
            policy ACCEPT;
            proto tcp dport 22 ACCEPT;
        }
        chain OUTPUT policy ACCEPT;
    }
}
