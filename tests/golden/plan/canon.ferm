domain ip table filter {
    chain INPUT {
        policy ACCEPT;
        saddr 10.0.0.1/32 proto tcp dport 22 ACCEPT;
        proto tcp sports (1024 1025) dports (80 443) ACCEPT;
    }
    chain FORWARD policy DROP;
    chain OUTPUT policy ACCEPT;
}
